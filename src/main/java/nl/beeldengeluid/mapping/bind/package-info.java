/**
 * Custom XML Wrappers. We needed one e.g. to have {@code xsd:anyURI} be a {@code java.net.URI} (and by called by {@link nl.beeldengeluid.mapping.impl.JaxbLeafMapper}.
 */
package nl.beeldengeluid.mapping.bind;
